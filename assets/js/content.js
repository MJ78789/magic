const grid=document.querySelector("#article-grid");const filters=document.querySelector("#category-filters");const dateFormat=new Intl.DateTimeFormat("th-TH",{dateStyle:"long"});
const card=item=>`<article class="article-card reveal visible"><a class="article-cover" href="article.html?slug=${encodeURIComponent(item.slug)}"><img src="${item.image}" alt="${item.imageAlt}" width="1600" height="900" loading="lazy"><span>${item.category}</span></a><div><p class="video-meta">${dateFormat.format(new Date(item.date))} · อ่าน ${item.readingTime}</p><h2><a href="article.html?slug=${encodeURIComponent(item.slug)}">${item.title}</a></h2><p>${item.excerpt}</p><a class="text-link" href="article.html?slug=${encodeURIComponent(item.slug)}">อ่านบทความ →</a></div></article>`;
fetch("data/articles.json").then(r=>r.json()).then(data=>{const items=data.items||[];const categories=["ทั้งหมด",...(data.categories||[])];const render=category=>{grid.innerHTML=items.filter(item=>category==="ทั้งหมด"||item.category===category).map(card).join("");filters.querySelectorAll("button").forEach(button=>button.classList.toggle("active",button.dataset.category===category))};filters.innerHTML=categories.map((category,index)=>`<button class="filter-button${index===0?" active":""}" type="button" data-category="${category}">${category}</button>`).join("");filters.addEventListener("click",event=>{const button=event.target.closest("button");if(button)render(button.dataset.category)});render("ทั้งหมด")}).catch(()=>{grid.innerHTML="<p>ไม่สามารถโหลดรายการบทความได้</p>"});
