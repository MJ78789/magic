const grid=document.querySelector("#article-grid");
fetch("data/articles.json").then(r=>r.json()).then(data=>{grid.innerHTML=(data.items||[]).map((item,index)=>`<article class="article-card reveal visible"><div class="article-art art-${(index%3)+1}"><span>${item.category}</span></div><div><p class="video-meta">${new Intl.DateTimeFormat("th-TH",{dateStyle:"long"}).format(new Date(item.date))} · อ่าน ${item.readingTime}</p><h2><a href="article.html?slug=${encodeURIComponent(item.slug)}">${item.title}</a></h2><p>${item.excerpt}</p><a class="text-link" href="article.html?slug=${encodeURIComponent(item.slug)}">อ่านบทความ →</a></div></article>`).join("")}).catch(()=>{grid.innerHTML="<p>ไม่สามารถโหลดรายการบทความได้</p>"});
