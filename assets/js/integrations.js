const root=document.querySelector("#integration-status");
try{const response=await fetch("/api/public/config",{headers:{accept:"application/json"}});if(!response.ok)throw Error();const data=await response.json();root.innerHTML=Object.entries(data.services).map(([name,value])=>`<div class="service"><span>${name}</span><strong>${value.available?"พร้อมใช้งาน":"ยังไม่เชื่อมต่อ"}</strong></div>`).join("")}catch{root.innerHTML='<p class="notice">เว็บไซต์นี้โฮสต์แบบ Static บน GitHub Pages จึงยังไม่เปิด Sign-in, Admin, Member, Newsletter หรือ Payment จนกว่าจะตั้งค่า backend ที่ปลอดภัย</p>'}
