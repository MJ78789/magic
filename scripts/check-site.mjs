import {readFile,readdir,stat} from "node:fs/promises";import path from "node:path";
const root=process.cwd();const publicPages=["index.html","youtube.html","content.html","faqs.html","products.html","search.html","legal.html","services.html","governance.html"];
const failures=[];
for(const file of publicPages){const html=await readFile(path.join(root,file),"utf8");if(!/<title>[^<]+<\/title>/.test(html))failures.push(`${file}: missing title`);if(!/<meta name="description"/.test(html))failures.push(`${file}: missing description`);if(!/<link rel="canonical"/.test(html))failures.push(`${file}: missing canonical`);if(!/href=["'](?:\/)?legal\.html/.test(html)&&file!=="legal.html")failures.push(`${file}: missing legal link`)}
const faq=JSON.parse(await readFile(path.join(root,"data/faqs.json"),"utf8"));if(faq.items.length!==faq.published)failures.push("FAQ published count mismatch");if(faq.target!==180)failures.push("FAQ target must remain 180");for(const item of faq.items){for(const key of ["Role:","Task:","Context:","Format:","Constraints:"])if(!item.prompt.includes(key))failures.push(`${item.id}: prompt missing ${key}`)}
const products=JSON.parse(await readFile(path.join(root,"data/products.json"),"utf8"));if(products.items.length)failures.push("Product data requires manual verification; automated MVP expects no listings");
const htmlFiles=await readdir(root);for(const file of htmlFiles.filter(x=>x.endsWith(".html"))){const html=await readFile(path.join(root,file),"utf8");for(const match of html.matchAll(/(?:href|src)="([^"#]+)"/g)){const ref=match[1];if(/^(?:https?:|mailto:|data:|\/api\/)/.test(ref)||ref.startsWith("/"))continue;const target=path.join(root,ref.split("?")[0]);try{await stat(target)}catch{failures.push(`${file}: broken local reference ${ref}`)}}}
if(failures.length){console.error(failures.join("\n"));process.exit(1)}console.log(`Site checks passed: ${publicPages.length} pages, ${faq.published}/${faq.target} FAQs, ${products.items.length} product listings`);
